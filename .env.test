# Default to MULTIUSER mode for tests since test fixtures assume multiuser routing
APP_MODE=MULTIUSER

# Dummy key so OpenAI-dependent code paths run deterministically in tests.
# All OpenAI services return canned responses in the test environment
# (see e.g. ModerationService#test_response) — no network calls are made.
OPENAI_API_KEY=test-key
