<div class="flex items-center">
  <button
    aria-label="<%= t 'search' %>"
    data-action="click->search-palette#open"
  >
    <%= heroicon "magnifying-glass", options: { class: "w-5 h-5 fill-gray-700 dark:fill-gray-300 hover:fill-black dark:hover:fill-white", "aria-hidden" => true, variant: :mini } %>
  </button>
</div>